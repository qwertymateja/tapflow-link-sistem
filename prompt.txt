Fix these issues:

1. MOBILE ZOOM FIX:
Add this to the <head> in layout.tsx:
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1" />
This prevents the initial zoom on mobile.

2. MOBILE PREVIEW BUTTON:
On mobile, add a "Preview" button in the dashboard (bottom of screen or in the header).
When clicked, opens a full-screen modal showing exactly how the public page looks.
No need to publish to see changes — it reads from current unsaved state.
Add a close button (X) to exit the preview modal.

3. PUBLIC PAGE LAYOUT:
Move the avatar and name closer to the top of the page.
Instead of centering everything vertically in the middle of the screen,
align content to start from ~10-15% from the top (padding-top: 10vh or similar).
Keep horizontal centering but remove vertical centering.

4. BACKGROUND IMAGE WITH CONTROLS:
In Dashboard → Design → Background section, add:
- Image upload option (upload to Supabase storage, bucket: backgrounds)
- URL input for background image
- Transparency/opacity slider (0% to 100%) for the background image
- Darkness overlay slider (0% to 80%) — adds a dark overlay on top of the image
  so text stays readable
- These sliders update the live preview in real time
- Save the values (image_url, image_opacity, image_overlay_darkness) 
  to the profiles table in Supabase

Also add the SQL for new columns:
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS bg_image_url text;
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS bg_image_opacity integer DEFAULT 100;
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS bg_image_overlay integer DEFAULT 0;