Add a new link type: PDF

TYPE 5 - PDF:
- Title (shown as button, e.g. "Our Menu")
- PDF file upload (upload to Supabase storage, bucket: pdfs)
- Or paste a URL to an existing PDF
- When clicked on public page → opens the PDF in a new tab or in a modal viewer
- Show a PDF icon/badge in the link list in dashboard
- In the modal: show PDF embedded with <iframe> or <embed>, with a "Download" button and "Open in new tab" button
- On mobile: since iframe PDF rendering is unreliable on mobile, 
  if on mobile device open the PDF directly in a new tab instead

Add to links table:
ALTER TABLE links ADD COLUMN IF NOT EXISTS pdf_url text;

Create a public Supabase storage bucket named: pdfs
Add storage policies same as avatars bucket but for pdfs bucket.